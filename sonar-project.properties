# Información del proyecto
sonar.projectKey=OctavioCarpineti_IngSWIII-TP07-Quality
sonar.organization=octaviocarpineti

# Nombre y versión
sonar.projectName=IngSWIII-TP07-Quality
sonar.projectVersion=1.0

# Directorios de código fuente (TODO el proyecto)
sonar.sources=backend,frontend/src

# Exclusiones MÍNIMAS (solo node_modules y build artifacts)
sonar.exclusions=**/node_modules/**,**/build/**,**/dist/**,**/coverage/**

# Tests
sonar.tests=backend/tests,frontend/src
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*_test.go

# Coverage reports
sonar.go.coverage.reportPaths=backend/coverage.out
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info

# Lenguajes
sonar.language=go,js,ts

# Encoding
sonar.sourceEncoding=UTF-8